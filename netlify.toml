[build]
  command = "npm install --include=dev && chmod +x node_modules/.bin/vite && npm run build"
  publish = "dist"

[build.environment]
  NPM_CONFIG_PRODUCTION = "false"
  NODE_ENV = "development"
